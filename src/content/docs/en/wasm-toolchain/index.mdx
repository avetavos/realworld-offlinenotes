---
title: Rust → WASM Toolchain
description: "Compile Rust to WASM with wasm-bindgen and call it from TypeScript."
sidebar:
  order: 0
---

Set up the Rust→WASM toolchain: compile a Rust crate with wasm-bindgen/wasm-pack and call it from TypeScript across the JS↔WASM boundary.

Lessons for this module are on the way — follow the sidebar as they land.
