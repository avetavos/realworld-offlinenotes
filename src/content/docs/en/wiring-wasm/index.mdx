---
title: Wiring the WASM Core
description: "Feed local edits through the WASM CRDT and persist its ops to IndexedDB."
sidebar:
  order: 0
---

Integrate the WASM CRDT into the note store: local edits become ops, ops persist to IndexedDB, and the UI renders from the merged state.

Start with [First Lesson →](/offlinenotes/en/wiring-wasm/edits-to-ops/), then work through the lessons in order (see the sidebar).
