---
title: Deployment
description: "Ship the PWA and the sync server, and pass the installability checks."
sidebar:
  order: 0
---

Deploy OfflineNotes: the static PWA (with its Service Worker) and the thin sync server, and pass the installability/PWA checks.

Start with [First Lesson →](/offlinenotes/en/deployment/ship-the-pwa/), then work through the lessons in order (see the sidebar).
