---
title: Service Worker (Offline)
description: "Precache the app shell so OfflineNotes works fully offline and installs as a PWA."
sidebar:
  order: 0
---

Add a Service Worker that precaches the app shell and assets, so OfflineNotes works with no network and installs as a PWA.

Start with [First Lesson →](/offlinenotes/en/service-worker/precaching-the-shell/), then work through the lessons in order (see the sidebar).
